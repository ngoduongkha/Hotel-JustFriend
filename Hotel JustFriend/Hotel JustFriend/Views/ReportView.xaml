<UserControl x:Class="Hotel_JustFriend.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_JustFriend.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <StackPanel>
                <TextBox 
                     Text="{Binding Month,Mode=OneWayToSource}"></TextBox>
                <TextBox Text="{Binding Year,Mode=OneWayToSource}"
                     ></TextBox>
                <Button Content="báo cáo"
                        Command="{Binding reportCommand}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
